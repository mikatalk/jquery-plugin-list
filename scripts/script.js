
$(function(){

  'use strict';

  var data = { tiles:[
  	// {''}
  ]};

	$(".list.mixed").list({
		mode: 'mixed'
	});

	$(".list.fixed").list({
		mode: 'fixed'
	});


	$(".list.column").list({
		mode: 'column'
	});

});
